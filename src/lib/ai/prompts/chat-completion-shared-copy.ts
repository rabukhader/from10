/**
 * Short messages fed back to the model on retry rounds (shared across grading + extraction).
 */

export const CHAT_RETRY_EMPTY_ASSISTANT_MESSAGE =
  "The assistant message was empty.";

export const CHAT_RETRY_JSON_PARSE_MESSAGE =
  "JSON parse failed. Ensure output is one JSON object only.";
